{
  "name": "@onepledge/contract",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "compact": "compact compile +0.31.1 src/registry.compact src/managed/registry",
    "compact:skip-zk": "compact compile +0.31.1 --skip-zk src/registry.compact src/managed/registry",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
